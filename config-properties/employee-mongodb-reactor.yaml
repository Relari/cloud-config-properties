spring:
  data:
    mongodb:
      database: db_demo
      host: localhost
      port: 27017

application:
  http-client:
    employee:
      base-url: http://localhost:${server.port}${application.api.path}

server:
  port: 9040

logging:
  level:
    com.pe.relari: TRACE
